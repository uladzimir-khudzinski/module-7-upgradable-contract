{
  "name": "upgradeable-erc20-project",
  "version": "1.0.0",
  "description": "Upgradeable ERC20 Token - Module 7 Assignment",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:core": "npx hardhat run scripts/deploy.js --network coreTestnet",
    "upgrade:core": "npx hardhat run scripts/upgrade.js --network coreTestnet",
    "interact:core": "npx hardhat run scripts/check.js --network coreTestnet",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.0"
  }
}

